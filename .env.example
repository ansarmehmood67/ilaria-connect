# Backend Configuration
# Set this to your public tunnel URL (ngrok, cloudflared, etc.)
VITE_BACKEND_URL=https://ea4a5c02815a.ngrok-free.app

# Streaming (optional - set to "true" when backend supports SSE/WebSocket)
VITE_ENABLE_STREAMING=false

# Language (auto-detect by default, or force "it" or "en")
VITE_DEFAULT_LOCALE=auto
